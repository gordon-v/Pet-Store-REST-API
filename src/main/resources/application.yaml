spring:
  application:
    name: PetStore
  jpa:
    database:
    properties:
      hibernate.format_sql: true
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:postgresql://localhost:5432/petstore
    username: petstore
    password: petstore